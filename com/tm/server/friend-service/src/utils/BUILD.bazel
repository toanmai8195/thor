load("//friend-service:ts.bzl", "ts_layer")

# Utils: hàm dùng chung (lỗi, event_id, thời gian, logger, Lifecycle), không phụ thuộc layer khác.
ts_layer(
    name = "utils",
    visibility = ["//friend-service:__subpackages__"],
    deps = ["//friend-service:node_modules/awilix"],
)
